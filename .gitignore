.DS_Store
*.exe
tmp/*
dump/*

!scripts/build
*.json

# Test artifacts
node_modules/
playwright-report/
test-results/
# Prevent accidental nested clone
espyna-golang/

# Secrets — untracked 2026-06-30 (keep local, never commit).
# NOTE: prior commits still contain .env in history — rotate exposed secrets.
.env
